# Paste this into the implementation LLM

Build a project-agnostic local AI council application on top of our existing repository, https://github.com/Tiago-0liveira/agy-manager. Extend the existing account manager with reusable council capabilities for arbitrary projects. Read BLUEPRINT.md, CONTRACTS.md, ACCEPTANCE.md and SOURCES_AND_REPO_REVIEW.md from this handoff package before implementation. The examples are proposed configuration contracts; they are not existing executable features.

The owner wants to connect any number of legitimate Antigravity account profiles, name them, choose a subset for a task, assign customizable agent roles/working styles and per-run duties, select models, run configurable collaborative workflows and inspect/recover/export the work. Do not hard-code four accounts, four workers, Gemini-only role assumptions or any particular domain. Separate Account, AgentTemplate, Worker, WorkflowTemplate, Run, Conversation and Artifact.

First inspect current repository HEAD and applicable local instructions. This design was based on commit 76bfbb562677ce74b37040bf8c03a205cd0456d6; do not assume the repository has not changed. Preserve all existing agym commands, stored profiles, subscription metadata, usage features and tests. Work in an isolated branch/worktree if appropriate. Do not alter live account credentials, delete profiles, log in, run billable model calls or launch the owner's real research simply to implement the application. Use a fake provider and temporary test profiles first; real sign-in and live trials require the owner's explicit action/authorization.

Use the existing profile/launcher primitives through a narrow service. They remain responsible for account environment construction. Add the council as an optional dependency extra and separate `agym-council` entry point so an existing profile called `council` continues to work. Update packaging deliberately: current setuptools lists only `agym`, so new subpackages/resources must be included and checked in an installed-wheel smoke test.

Recommended stack: Python backend retaining current base compatibility, typed contracts, SQLite and asyncio; FastAPI for a local API; React/TypeScript/Vite for a practical browser UI. No cloud backend, Redis, multi-machine cluster, vector database, marketplace or generic agent framework is required initially. The provider interface should allow future CLI/API adapters without changing the workflow engine. Existing council repositories may inform small reviewed components, but do not merge their entire applications.

Implement in demonstrable vertical slices:

1. Domain contracts, migrations and fake provider; keep existing tests passing. Validate unique workers, profile references, stage ordering, input visibility, output expectations, limits and account/model bindings.
2. Official Antigravity headless adapter using explicit conversation IDs, structured results, controlled cwd/environment, cancellation and retained diagnostic output. Probe installed capabilities; do not assume a particular version/flag/model works. Do not use 'latest conversation.' No implicit permission bypass from inherited environment/profile defaults.
3. Durable ordered-stage engine with parallel workers, release barriers, fixed role assignments, per-account/global capacity, finite budgets, retries, pause/stop and crash reconciliation. Initially use explicit finite stages; do not invent an unlimited autonomous debate loop. Unknown completion is not safe-to-retry by default, particularly for side-effecting work.
4. Local UI and account connection flow. Add accounts through native provider authentication with an explicit user-launched helper; no embedded password capture. Show auth verification separately from credential-file presence. Support role editing, selectable team size, model binding, workflow stage editing, run launch/review, live progress, artifacts, dissent, pause/resume and export.
5. After the core works, add bounded revision blocks, registered tool jobs, scoped workspace implementation and optional external auditor providers. Keep these out of the initial implementation if their safety/recovery semantics are not ready. Show unsupported capabilities honestly.

For the first useful release, prove a configurable council can produce independent submissions, exchange them for criticism, synthesize while preserving disagreement, recover after interruption and export its complete work. Also support one worker and larger fake-provider teams. A visually polished but non-recoverable chat room is not an acceptable substitute.

Implement effective access control, not merely prompt wording. Run-role text cannot grant tools or remove data restrictions. Isolated profile homes are not file-access sandboxes. If enforced confinement is unavailable, label execution honestly and refuse workflows that require protected-data guarantees. Begin with supplied-evidence reasoning and explicit local permissions. Keep tokens/auth files out of the council database and exports. Bind the web service to loopback with origin/session protection. Imported workflow files are declarative data, not arbitrary executable commands.

Lock all profile read/modify/write operations across both existing CLI and council before supporting concurrent profile mutation. Do not hold that metadata lock for the duration of a long-running model task. Account mutations affecting active workers must be handled explicitly. Normal profile launch behavior remains compatible.

Use ACCEPTANCE.md to drive meaningful tests. Prioritize independence barriers, duplicate models with different worker IDs, account isolation, result parsing, unknown completion, idempotent API actions, stopped process trees, output provenance and backward compatibility. Do not claim tests were run if they were only designed. Keep status, failures and limitations visible to the owner.

Deliver: working code and appropriate tests; documented setup/launch path; runnable fake-provider demo; three reusable presets; import/export examples; a concise architecture note; what was actually verified; and the small manual real-account checklist still needed. Do not silently stop at a static UI mockup. If the task must span turns, finish the current vertical slice, record the exact remaining work and continue under the owner's execution context.

Make routine implementation decisions autonomously within this scope. Ask only when a material user preference, credential interaction or external action genuinely cannot be inferred. Do not ask the owner to approve every internal stage. Keep domain-specific concepts in optional workflow presets. The goal is a small dependable application that can grow through real use.
